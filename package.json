{
  "name": "logicgraph",
  "private": true,
  "version": "0.1.0",
  "description": "Persistent, version-controlled application behavior for AI coding agents.",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "license": "MIT"
}
